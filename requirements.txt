# 🚀 pipsmade Investment Platform - Production Requirements
# Essential packages needed to run the project in production

# =============================================================================
# CORE FRAMEWORK (REQUIRED)
# =============================================================================
Django==5.2.2                    # Web framework - Latest stable version

# =============================================================================
# DATABASE (PRODUCTION)
# =============================================================================
psycopg2-binary==2.9.7           # PostgreSQL adapter (recommended for production)

# =============================================================================
# WEB SERVER (PRODUCTION)
# =============================================================================
gunicorn==21.2.0                 # WSGI HTTP Server
whitenoise==6.6.0                # Static file serving

# =============================================================================
# CACHING & PERFORMANCE (PRODUCTION)
# =============================================================================
redis==5.0.1                     # Redis for caching and sessions
django-redis==5.4.0              # Redis cache backend for Django

# =============================================================================
# TASK QUEUE (PRODUCTION)
# =============================================================================
celery==5.3.4                    # Asynchronous task queue
django-celery-beat==2.5.0        # Periodic task scheduling
django-celery-results==2.5.1     # Store Celery results in Django

# =============================================================================
# EMAIL (PRODUCTION)
# =============================================================================
django-anymail==10.2              # Email backend abstraction
sendgrid==6.10.0                 # SendGrid email service

# =============================================================================
# SECURITY (PRODUCTION)
# =============================================================================
django-ratelimit==4.1.0          # Rate limiting for security
cryptography==41.0.7             # Cryptographic recipes
django-otp==1.3.0                # Two-factor authentication

# =============================================================================
# MONITORING & LOGGING (PRODUCTION)
# =============================================================================
sentry-sdk==1.38.0               # Error tracking and monitoring
structlog==23.2.0                # Structured logging
django-health-check==3.17.0      # Health check endpoints

# =============================================================================
# STATIC FILES & MEDIA (PRODUCTION)
# =============================================================================
django-storages==1.14.2          # Custom storage backends
boto3==1.34.0                    # AWS S3 storage
Pillow==10.1.0                   # Image processing

# =============================================================================
# UTILITIES (PRODUCTION)
# =============================================================================
python-dateutil==2.8.2           # Date utilities
pytz==2023.3                     # Timezone support
requests==2.31.0                 # HTTP library for API calls

# =============================================================================
# ENVIRONMENT & CONFIGURATION (PRODUCTION)
# =============================================================================
python-decouple==3.8             # Environment variable management
django-environ==0.11.2           # Environment configuration

# =============================================================================
# API & SERIALIZATION (PRODUCTION)
# =============================================================================
djangorestframework==3.14.0      # REST API framework
django-cors-headers==4.3.1       # CORS handling for API endpoints
djangorestframework-simplejwt==5.3.0  # JWT authentication for API

# =============================================================================
# AUTHENTICATION & SECURITY (PRODUCTION)
# =============================================================================
django-allauth==0.57.0           # Advanced authentication system
django-crispy-forms==2.1          # Better form rendering
crispy-bootstrap5==0.7            # Bootstrap 5 integration for crispy forms

# =============================================================================
# PRODUCTION & DEPLOYMENT (PRODUCTION)
# =============================================================================
django-prometheus==2.3.1         # Prometheus metrics

# =============================================================================
# CRYPTO & FINANCIAL (PRODUCTION)
# =============================================================================
pycryptodome==3.19.0             # Cryptographic library
ccxt==4.1.77                     # Cryptocurrency trading library

# =============================================================================
# TEMPLATE & FRONTEND (PRODUCTION)
# =============================================================================
django-compressor==4.4            # CSS/JS compression
django-pipeline==1.7.0           # Asset pipeline

# =============================================================================
# SEARCH & INDEXING (PRODUCTION)
# =============================================================================
django-haystack==3.2.1           # Search framework
whoosh==2.7.4                    # Pure Python search engine

# =============================================================================
# INSTALLATION COMMANDS
# =============================================================================
# Production installation:
# pip install -r requirements.txt
#
# Create virtual environment:
# python -m venv venv
# source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate     # Windows
#
# Install production dependencies:
# pip install -r requirements.txt

# =============================================================================
# PRODUCTION SETUP NOTES
# =============================================================================
# 1. Set DEBUG=False in settings.py
# 2. Configure SECRET_KEY from environment variable
# 3. Set ALLOWED_HOSTS for your domain
# 4. Configure database (PostgreSQL recommended)
# 5. Set up Redis for caching
# 6. Configure email backend (SendGrid)
# 7. Set up static file serving (Nginx + Gunicorn)
# 8. Configure logging and monitoring
# 9. Set up SSL/TLS certificates
# 10. Configure backup and recovery procedures 